// Breakpoints centralizados para todas as media queries do app.
// Use sempre estes valores em vez de números soltos, para manter consistência
// entre as páginas (celulares pequenos, celulares grandes/tablets, notebooks e telas cheias).
export const breakpoints = {
  mobile: "480px", // celulares pequenos
  tablet: "768px", // celulares grandes / tablets em pé
  laptop: "1024px", // tablets deitados / notebooks menores
  desktop: "1300px", // telas cheias (o design original foi pensado para esse tamanho)
};

export default breakpoints;
